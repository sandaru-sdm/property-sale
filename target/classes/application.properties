spring.application.name=property-sale

# DataSource configuration
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USER}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Security configuration
application.security.jwt.secret-key=${JWT_SECRET_KEY}
application.security.jwt.expiration=86400000

server.port=8080

app.client.url=${FRONTEND_URL}
app.base.url=${BASE_URL}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=60MB
spring.servlet.multipart.file-size-threshold=1MB

# Create uploads directory on startup
spring.web.resources.static-locations=classpath:/static/,file:uploads/

# Static resource handling
spring.mvc.static-path-pattern=/uploads/**

# Property file upload directory
property.image.upload.dir=uploads/properties/

# Default Admin Credentials
admin.default.email=maduhansadilshan@gmail.com
admin.default.name=Sandaru Gunathilake
admin.default.mobile=0701794934
admin.default.password=dilshan2000